/*
package test.pageObj;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

*/
/**
 * @param
 * @Auther: zhangcheng
 * @Date: 2020/8/27 15:53
 * @Description:编写操作的元素定位方式和控件操作
 *//*

public class LoginPageObj {
     WebDriver webDriver;

    public LoginPageObj( WebDriver webDriver ) {
        this.webDriver = webDriver;
    }

    //定位 用户名
    public static By userNameInput = By.name("loginname");
    public static By passWordInput = By.name("loginpassword");
    public static By loginBtn = By.linkText("立即登录");
    //定位 获取错误信息//
    public static By errorMsg = By.
            cssSelector("[class='b-signin-error js-b-signin-error error-2']");


    //登录
    public void sendKeysUserName(String userName) {
        webDriver.findElement(userNameInput).clear();
        webDriver.findElement(userNameInput).sendKeys(userName);
    }
   //输入密码
    public void sendKeysPassWord(String passWord) {
        webDriver.findElement(passWordInput).clear();
        webDriver.findElement(passWordInput).sendKeys(passWord);

    }

}
*/
